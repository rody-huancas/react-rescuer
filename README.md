# react-rescuer

Smart React error boundaries with recovery, observability, and DX.

This repo is a pnpm workspace with:

- `packages/lib` - the published npm package `react-rescuer`
- `packages/playground` - a Vite app for manual testing
